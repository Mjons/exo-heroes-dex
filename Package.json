{
  "name": "exo-heroes-dex",
  "version": "1.0.0",
  "description": "ExO Heroes Explorer application",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd frontend && npm install && npm run build && cd ../backend && npm install"
  },
  "engines": {
    "node": "14.x"
  }
}